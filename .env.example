# Supabase Configuration
# The library uses the 'taxonomy' schema (not public)

# Production/Remote Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key
SUPABASE_ANON_KEY=your-anon-key

# Local Development (takes precedence if set)
# Add _DEV suffix for local Supabase - these are checked first
# SUPABASE_URL_DEV=http://localhost:54321
# SUPABASE_SERVICE_KEY_DEV=your-local-service-key
# SUPABASE_ANON_KEY_DEV=your-local-anon-key

# OpenAI (for generating embeddings)
OPENAI_API_KEY=sk-your-api-key
